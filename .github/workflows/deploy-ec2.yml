name: Deploy to AWS EC2

on:
  push:
    branches:
      - production
  workflow_dispatch:  # Allows manual trigger

jobs:
  build-and-deploy:
    name: Build and Deploy Backend to EC2
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: Backend/package-lock.json
      
      - name: Install dependencies
        working-directory: Backend
        run: |
          echo "📦 Installing dependencies..."
          npm ci
      
      - name: Build TypeScript
        working-directory: Backend
        run: |
          echo "🏗️ Building TypeScript..."
          npm run build
          echo "✅ Build complete!"
          echo "📁 Contents of dist folder:"
          ls -la dist/
      
      - name: Create deployment package
        working-directory: Backend
        run: |
          echo "📦 Creating deployment package..."
          tar -czf dist.tar.gz dist/
          echo "✅ Package created:"
          ls -lh dist.tar.gz
      
      - name: Verify dist.tar.gz exists
        working-directory: Backend
        run: |
          echo "🔍 Verifying dist.tar.gz exists before upload..."
          if [ ! -f dist.tar.gz ]; then
            echo "❌ ERROR: dist.tar.gz not found in Backend directory!"
            exit 1
          fi
          echo "✅ dist.tar.gz found:"
          ls -lh dist.tar.gz
      
      - name: Ensure target directory exists on EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "📁 Ensuring target directory exists..."
            mkdir -p /home/ubuntu/Audio-extractor-Chrome-Extension/Backend
            echo "✅ Directory ready"
      
      - name: Verify file path for upload
        run: |
          echo "🔍 Verifying file exists at workspace root level..."
          ls -lh Backend/dist.tar.gz || echo "File not found at Backend/dist.tar.gz"
          pwd
          echo "📁 Workspace contents:"
          ls -la | head -10
      
      - name: Upload dist.tar.gz to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          source: "Backend/dist.tar.gz"
          target: "/home/ubuntu/Audio-extractor-Chrome-Extension/Backend/"
          strip_components: 1
          debug: true
          overwrite: true
          rm: false
        continue-on-error: false
      
      - name: Verify upload on EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "🔍 Verifying dist.tar.gz was uploaded successfully..."
            cd /home/ubuntu/Audio-extractor-Chrome-Extension/Backend || exit 1
            echo "📁 Current directory:"
            pwd
            echo "📁 Directory contents:"
            ls -la | head -20
            
            # Check both possible locations
            if [ -f dist.tar.gz ]; then
              echo "✅ dist.tar.gz found in current directory:"
              ls -lh dist.tar.gz
              echo "🔍 Verifying file integrity..."
              if [ -s dist.tar.gz ]; then
                echo "✅ File is not empty, size check passed"
              else
                echo "❌ ERROR: File is empty!"
                exit 1
              fi
            elif [ -f Backend/dist.tar.gz ]; then
              echo "✅ dist.tar.gz found in Backend/ subdirectory (will be moved during deployment):"
              ls -lh Backend/dist.tar.gz
              echo "🔍 Verifying file integrity..."
              if [ -s Backend/dist.tar.gz ]; then
                echo "✅ File is not empty, size check passed"
                echo "ℹ️ Note: File will be moved to current directory during deployment"
              else
                echo "❌ ERROR: File is empty!"
                exit 1
              fi
            else
              echo "❌ ERROR: dist.tar.gz not found on EC2 after upload!"
              echo "📁 Full directory contents:"
              ls -la
              if [ -d Backend ]; then
                echo "📁 Backend subdirectory contents:"
                ls -la Backend/ | head -10
              fi
              echo "🔍 Searching for dist.tar.gz..."
              find /home/ubuntu -name "dist.tar.gz" -type f 2>/dev/null | head -10
              exit 1
            fi
      
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "🚀 Starting deployment..."
            
            # Navigate to backend directory
            cd /home/ubuntu/Audio-extractor-Chrome-Extension/Backend || { echo "❌ Backend directory not found!"; exit 1; }
            
            echo "📁 Current working directory:"
            pwd
            
            # IMPORTANT: Extract dist.tar.gz FIRST before any git operations
            # (git clean might remove it if we do git operations first)
            echo "📦 Looking for dist.tar.gz..."
            
            # Check multiple possible locations (SCP might extract to Backend/Backend/)
            DIST_TAR=""
            if [ -f dist.tar.gz ]; then
              DIST_TAR="dist.tar.gz"
              echo "✅ Found dist.tar.gz in current directory"
            elif [ -f Backend/dist.tar.gz ]; then
              echo "✅ Found dist.tar.gz in Backend/ subdirectory, moving to current directory..."
              mv Backend/dist.tar.gz dist.tar.gz
              DIST_TAR="dist.tar.gz"
              echo "✅ File moved successfully"
            elif [ -f ./dist.tar.gz ]; then
              DIST_TAR="./dist.tar.gz"
              echo "✅ Found dist.tar.gz with ./ prefix"
            else
              echo "❌ ERROR: dist.tar.gz not found in expected location!"
              echo "📁 Current directory contents:"
              ls -la
              if [ -d Backend ]; then
                echo "📁 Backend subdirectory contents:"
                ls -la Backend/ | head -10
              fi
              echo "🔍 Searching for dist.tar.gz..."
              find /home/ubuntu -name "dist.tar.gz" -type f 2>/dev/null | head -5 || echo "No dist.tar.gz found anywhere"
              exit 1
            fi
            
            if [ -n "$DIST_TAR" ]; then
              echo "✅ Found dist.tar.gz, extracting..."
              rm -rf dist/  # Remove old dist if exists
              tar -xzf "$DIST_TAR"
              echo "✅ Dist folder extracted:"
              ls -la dist/
              
              # Verify extraction was successful
              if [ ! -d dist ] || [ ! -f dist/server.js ]; then
                echo "❌ ERROR: dist folder extraction failed or server.js not found!"
                exit 1
              fi
              
              echo "🔍 Verifying /auth/latest endpoint after extraction..."
              if grep -q "auth/latest" dist/server.js; then
                echo "✅ /auth/latest endpoint verified in extracted dist/server.js"
              else
                echo "⚠️ WARNING: /auth/latest endpoint not found, but continuing..."
              fi
            fi
            
            echo "📥 Pulling latest code from production branch..."
            git fetch origin
            git checkout production
            # Handle untracked files that might conflict (but preserve dist.tar.gz and dist/)
            git clean -fd --exclude=dist.tar.gz --exclude=dist/ || true
            git pull origin production || {
              echo "⚠️ Git pull had conflicts, stashing local changes..."
              git stash || true
              git pull origin production || true
            }
            
            # Clean up dist.tar.gz after git operations
            rm -f dist.tar.gz
            
            echo "🔧 Setting up Dockerfile..."
            if [ -f Dockerfile.fast ]; then
              cp Dockerfile.fast Dockerfile
              echo "✅ Using Dockerfile.fast"
            else
              echo "⚠️ Dockerfile.fast not found, using existing Dockerfile"
            fi
            
            echo "🛑 Stopping existing containers..."
            docker-compose -f docker-compose.prod.yml down || true
            
            echo "🧹 Cleaning up Docker (free space)..."
            docker system prune -f || true
            
            echo "🏗️ Building Docker image..."
            docker-compose -f docker-compose.prod.yml build --no-cache
            
            echo "🚀 Starting containers..."
            docker-compose -f docker-compose.prod.yml up -d
            
            echo "⏳ Waiting for container to be healthy..."
            sleep 10
            
            echo "🧹 Cleaning up unused Docker images..."
            docker image prune -f || true
            
            echo "📊 Container status:"
            docker-compose -f docker-compose.prod.yml ps
            
            echo "📋 Recent logs:"
            docker-compose -f docker-compose.prod.yml logs --tail=30
            
            echo "🏥 Health check..."
            curl -f http://localhost:5000/drive/status || echo "⚠️ Health check failed, but deployment completed"
            
            echo "✅ Deployment complete!"

